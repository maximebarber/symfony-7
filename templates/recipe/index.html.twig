{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <h1>Recettes</h1>

    <ul>
        <li>
            <a href="{{ path('recipe.show', {id: 1, slug:'pates-bolo'}) }}">Pâtes bolo</a>
        </li>
    </ul>
{% endblock %}